import 'package:flutter/material.dart';

class Bartolome extends StatelessWidget {
  const Bartolome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Horario de Bartolome',
      style: TextStyle(fontSize: 16),
    );
  }
}
