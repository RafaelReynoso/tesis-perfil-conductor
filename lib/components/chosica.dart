import 'package:flutter/material.dart';

class Chosica extends StatelessWidget {
  const Chosica({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Horario de Chosica',
      style: TextStyle(fontSize: 16),
    );
  }
}
